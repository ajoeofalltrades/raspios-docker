# Accessibility Requirements
- If any CLI tool is wrapped in a UI, it must meet:
  - WCAG 2.1 AA conformance
  - ARIA-compliant dynamic content updates
  - Keyboard and screen reader navigation support